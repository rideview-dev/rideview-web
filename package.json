{
  "name": "rideview-web",
  "version": "1.1.0",
  "private": true,
  "description": "RideView marketing site and partner-form backend, deployed together as one app",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix server install --omit=dev",
    "start": "npm --prefix server run start",
    "dev": "npm --prefix server run dev",
    "check": "npm --prefix server run check",
    "mail:test": "npm --prefix server run mail:test --"
  }
}
